# chill out mission specific pip reqs only to create a build with less security issues
DIR=${1:-$IMAGE_DIR}

cp $DIR/env-frozen $DIR/env-chilly

find ./env-chilly -type f -name "requirements.txt" -exec sed -i 's/==/~=/g' {} +