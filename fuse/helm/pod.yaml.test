apiVersion: v1
kind: Pod
metadata:
  name: s3-pvc-tester
spec:
  containers:
  - command:
    - sleep
    - infinity
    image: ubuntu
    name: ubuntu
    volumeMounts:
    - mountPath: /s3:shared
      name: s3-pvc
  volumes:
  - hostPath:
      path: /s3
    name: s3-pvc
